<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="UTF-8">
  <title>{{ shop.name }} – Password Protected</title>
  {{ content_for_header }}
</head>
<body>
  {{ content_for_layout }}
</body>
</html>